# 🌳 Banches Backend API

Бэкенд для Android-приложения с картой мест отдыха (скамейки, беседки).  
Реализован на **FastAPI** с использованием **PostgreSQL**, **SQLAlchemy** и **Alembic**.

---

## 🛠 Технологии
*   **Python 3.11+**
*   **FastAPI** — веб-фреймворк
*   **SQLAlchemy (Async)** — работа с базой данных
*   **Alembic** — миграции базы данных
*   **PostgreSQL** — база данных
*   **SQLAdmin** — админ-панель

---

## 🚀 Быстрый старт

# 1. Настройка окружения

Создайте виртуальное окружение и активируйте его:

## Windows
```
python -m venv venv
```
```
venv\Scripts\activate
```
## Linux/macOS
```
python3 -m venv venv
```
```
source venv/bin/activate
```
Установите зависимости:
```
pip install -r requirements.txt
```
# 2. Настройка Базы Данных
Убедитесь, что у вас установлен PostgreSQL и создана база данных (настройки подключения в .env или config.py).

Примените миграции (создание таблиц):

```
alembic upgrade head
```
Заполните базу тестовыми данными (Справочники, тестовые юзеры, точки):
```
python seed.py
```
(Опционально) Если нужно полностью очистить базу данных:
```
python clean.py
```
# 3. Запуск сервера

Запустите локальный сервер разработки:
```
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## 📚 Документация и Доступ

После запуска сервера доступны следующие адреса:

Интерактивная документация API: Swagger UI

```	
http://127.0.0.1:8000/docs
```

Панель управления данными: Admin Panel
```	
http://127.0.0.1:8000/admin
```

## 🔑 Тестовые доступы (из seed.py)

Администратор:

```
Login: admin@admin.com

Password: admin123
```
Пользователь:

```
Login: user@user.com

Password: user123
```
## 💻 Разработка и Миграции

Если вы изменили модели (models.py), нужно создать новую миграцию:

```
alembic revision --autogenerate -m "Описание изменений"

alembic upgrade head
```

Если вы установили новые библиотеки, обновите список зависимостей:

```
pip freeze > requirements.txt
```

